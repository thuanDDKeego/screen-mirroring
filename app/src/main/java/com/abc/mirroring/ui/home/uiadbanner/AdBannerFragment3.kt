package com.abc.mirroring.ui.home.uiadbanner

import android.view.LayoutInflater
import android.view.ViewGroup
import com.abc.mirroring.base.BaseFragment
import com.abc.mirroring.databinding.FragmentAdBanner3Binding

class AdBannerFragment3 : BaseFragment<FragmentAdBanner3Binding>() {

    override fun initBinding(
        inflater: LayoutInflater,
        container: ViewGroup?
    ) = FragmentAdBanner3Binding.inflate(inflater, container, false)

    override fun initViews() {
    }

    override fun initActions() {
    }
}